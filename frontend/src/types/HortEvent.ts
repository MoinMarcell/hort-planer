export type HortEvent = {
    id: string,
    title: string,
    description: string,
    startDateTime: string,
    endDateTime: string,
    createdAt: string,
}

export type HortEventDto = {
    title: string,
    description: string,
    startDateTime: string,
    endDateTime: string,
}
